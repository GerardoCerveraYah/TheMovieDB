<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="movie"
            type="com.pruebadacodes.movies.models.Movie" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <androidx.cardview.widget.CardView
            android:id="@+id/card_movie"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:clickable="true"
            android:elevation="@dimen/card_corner_elevation"
            android:focusable="true"
            android:foreground="?android:attr/selectableItemBackground"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintDimensionRatio="H,2:3"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_margin="@dimen/margin_view_default"
            app:cardCornerRadius="@dimen/card_corner_radius"
            app:cardElevation="@dimen/card_corner_elevation">
            <ImageView
            android:id="@+id/image_poster"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/image_content_description" />
            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom"
                android:background="@drawable/bg_image_overlay"
                android:padding="@dimen/padding_view_small">

                <TextView
                    android:id="@+id/text_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="@color/white"
                    android:textSize="@dimen/text_small"
                    android:layout_alignParentTop="true"
                    android:text="@{movie.title}"
                    android:paddingStart="@dimen/padding_view_small"
                    android:paddingEnd="@dimen/padding_view_small"
                    android:maxLines="1"
                    android:ellipsize="end"/>

                <TextView
                    android:id="@+id/text_release_date"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:textColor="@color/white"
                    android:textSize="@dimen/text_xsmall"
                    android:layout_below="@id/text_title"
                    android:layout_toStartOf="@id/image_star"
                    android:layout_alignParentStart="true"
                    android:text="@{movie.releaseDate}"/>

                <ImageView
                    android:id="@+id/image_star"
                    android:layout_width="@dimen/icon_list_width"
                    android:layout_height="@dimen/icon_list_height"
                    android:contentDescription="@string/image_content_description"
                    android:src="@drawable/ic_star"
                    android:layout_below="@id/text_title"
                    android:layout_toStartOf="@id/text_vote"
                    android:paddingStart="@dimen/margin_view_xsmall"
                    android:paddingEnd="@dimen/margin_view_xsmall"/>
                <TextView
                    android:id="@+id/text_vote"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@color/white"
                    android:textSize="@dimen/text_xsmall"
                    android:layout_below="@id/text_title"
                    android:layout_alignParentEnd="true"
                    android:text="@{Double.toString(movie.voteAverage)}"/>
            </RelativeLayout>
        </androidx.cardview.widget.CardView>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>